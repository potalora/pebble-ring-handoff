# Pebble Ring handoff for Hermes

The installable Hermes plugin is in [`package/`](package/). It requires a separate receiver and a private Discord channel. Read [the setup guide](package/README.md) before installing.

Development tests are in `tests/`. The plugin catalog pins a reviewed commit and uses `subdir: package`.
